{
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build:scala": "mkdir -p dist && scala-cli package --js --output dist -f .",
    "build": "npm run build:scala",
    "scp": "scp dist/* pi:/home/pi/docker/tgtg/",
    "all": "npm run build && npm run scp"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "node-fetch": "^3.3.1"
  }
}
