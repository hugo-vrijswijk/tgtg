{
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build:scala": "mkdir -p dist && scala-cli package --js --output dist/main.js -f .",
    "build": "npm run build:scala",
    "scp": "scp dist/main.js pi:/home/pi/docker/tgtg/index.js",
    "all": "npm run build && npm run scp"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "node-fetch": "^3.3.1"
  }
}
