{
  "type": "module",
  "main": "./index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "mkdir -p dist && scala-cli package --js --output dist/main.js -f .",
    "scp": "scp dist/* pi:/home/pi/docker/tgtg/",
    "all": "npm run build && npm run scp"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}
